<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huang.demo.excel.repository.StudentMapper">

    <resultMap id="StudentExcelRowMap" type="com.huang.demo.excel.model.StudentExcelRow">
        <result property="studentNo" column="student_no"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="gender" column="gender"/>
        <result property="className" column="class_name"/>
        <result property="email" column="email"/>
        <result property="birthday" column="birthday"/>
    </resultMap>

    <update id="createTableIfAbsent">
        CREATE TABLE IF NOT EXISTS student_record (
            id BIGINT AUTO_INCREMENT PRIMARY KEY,
            import_version BIGINT NOT NULL DEFAULT 1,
            import_task_id VARCHAR(64),
            student_no VARCHAR(32) NOT NULL,
            name VARCHAR(64) NOT NULL,
            age INT,
            gender VARCHAR(16),
            class_name VARCHAR(64),
            email VARCHAR(128),
            birthday VARCHAR(32),
            UNIQUE KEY uk_student_record_version_student_no (import_version, student_no),
            KEY idx_student_record_version_id (import_version, id),
            KEY idx_student_record_import_task_id (import_task_id)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    </update>

    <update id="createVersionControlTableIfAbsent">
        CREATE TABLE IF NOT EXISTS student_import_version_control (
            id BIGINT NOT NULL PRIMARY KEY,
            current_version BIGINT NOT NULL,
            created_at DATETIME NOT NULL,
            updated_at DATETIME NOT NULL
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    </update>

    <insert id="initVersionControl">
        INSERT IGNORE INTO student_import_version_control (id, current_version, created_at, updated_at)
        VALUES (1, 1, NOW(), NOW())
    </insert>

    <update id="ensureStudentRecordVersionColumns">
        ALTER TABLE student_record
            ADD COLUMN IF NOT EXISTS import_version BIGINT NOT NULL DEFAULT 1 AFTER id,
            ADD COLUMN IF NOT EXISTS import_task_id VARCHAR(64) NULL AFTER import_version
    </update>

    <update id="createImportStageTableIfAbsent">
        CREATE TABLE IF NOT EXISTS student_import_stage (
            id BIGINT AUTO_INCREMENT PRIMARY KEY,
            import_task_id VARCHAR(64) NOT NULL,
            row_no INT NOT NULL,
            student_no VARCHAR(256),
            name VARCHAR(256),
            age INT,
            gender VARCHAR(64),
            class_name VARCHAR(256),
            email VARCHAR(512),
            birthday VARCHAR(128),
            created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
            KEY idx_student_import_stage_task_row (import_task_id, row_no),
            KEY idx_student_import_stage_task_student_no (import_task_id, student_no)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    </update>

    <update id="updateImportStageColumnCapacity">
        ALTER TABLE student_import_stage
            MODIFY student_no VARCHAR(256),
            MODIFY name VARCHAR(256),
            MODIFY gender VARCHAR(64),
            MODIFY class_name VARCHAR(256),
            MODIFY email VARCHAR(512),
            MODIFY birthday VARCHAR(128)
    </update>

    <select id="countStudentNoUniqueIndex" resultType="int">
        SELECT COUNT(*)
        FROM information_schema.statistics
        WHERE table_schema = DATABASE()
          AND table_name = 'student_record'
          AND index_name = 'uk_student_record_version_student_no'
    </select>

    <select id="countLegacyStudentNoUniqueIndex" resultType="int">
        SELECT COUNT(*)
        FROM information_schema.statistics
        WHERE table_schema = DATABASE()
          AND table_name = 'student_record'
          AND index_name = 'uk_student_record_student_no'
    </select>

    <select id="countStudentVersionIdIndex" resultType="int">
        SELECT COUNT(*)
        FROM information_schema.statistics
        WHERE table_schema = DATABASE()
          AND table_name = 'student_record'
          AND index_name = 'idx_student_record_version_id'
    </select>

    <select id="countStudentImportTaskIdIndex" resultType="int">
        SELECT COUNT(*)
        FROM information_schema.statistics
        WHERE table_schema = DATABASE()
          AND table_name = 'student_record'
          AND index_name = 'idx_student_record_import_task_id'
    </select>

    <select id="countDuplicateStudentNo" resultType="int">
        SELECT COUNT(*)
        FROM (
            SELECT import_version, student_no
            FROM student_record
            GROUP BY import_version, student_no
            HAVING COUNT(*) &gt; 1
        ) duplicate_student_no
    </select>

    <update id="dropLegacyStudentNoUniqueIndex">
        DROP INDEX uk_student_record_student_no ON student_record
    </update>

    <update id="createStudentNoUniqueIndex">
        ALTER TABLE student_record
            ADD UNIQUE KEY uk_student_record_version_student_no (import_version, student_no)
    </update>

    <update id="createStudentVersionIdIndex">
        ALTER TABLE student_record
            ADD KEY idx_student_record_version_id (import_version, id)
    </update>

    <update id="createStudentImportTaskIdIndex">
        ALTER TABLE student_record
            ADD KEY idx_student_record_import_task_id (import_task_id)
    </update>

    <select id="currentStudentVersion" resultType="long">
        SELECT current_version
        FROM student_import_version_control
        WHERE id = 1
        LIMIT 1
    </select>

    <update id="promoteStudentVersion">
        UPDATE student_import_version_control
        SET current_version = #{newVersion},
            updated_at = NOW()
        WHERE id = 1
          AND current_version = #{expectedVersion}
    </update>

    <sql id="CurrentStudentVersionCondition">
        import_version = (SELECT current_version FROM student_import_version_control WHERE id = 1)
    </sql>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM student_record
        WHERE <include refid="CurrentStudentVersionCondition"/>
    </select>

    <select id="listPage" resultMap="StudentExcelRowMap">
        SELECT student_no, name, age, gender, class_name, email, birthday
        FROM student_record
        WHERE <include refid="CurrentStudentVersionCondition"/>
        ORDER BY id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="maxId" resultType="long">
        SELECT MAX(id) FROM student_record
        WHERE <include refid="CurrentStudentVersionCondition"/>
    </select>

    <sql id="StudentExportQueryCondition">
        <if test="query != null and query.studentNo != null and query.studentNo != ''">
            AND student_no = #{query.studentNo}
        </if>
        <if test="query != null and query.nameKeyword != null and query.nameKeyword != ''">
            AND name LIKE CONCAT('%', #{query.nameKeyword}, '%')
        </if>
        <if test="query != null and query.className != null and query.className != ''">
            AND class_name = #{query.className}
        </if>
        <if test="query != null and query.gender != null and query.gender != ''">
            AND gender = #{query.gender}
        </if>
        <if test="query != null and query.minAge != null">
            AND age &gt;= #{query.minAge}
        </if>
        <if test="query != null and query.maxAge != null">
            AND age &lt;= #{query.maxAge}
        </if>
    </sql>

    <select id="maxIdByQuery" resultType="long">
        SELECT MAX(id)
        FROM student_record
        WHERE <include refid="CurrentStudentVersionCondition"/>
        <include refid="StudentExportQueryCondition"/>
    </select>

    <select id="maxIdByVersionAndQuery" resultType="long">
        SELECT MAX(id)
        FROM student_record
        WHERE import_version = #{version}
        <include refid="StudentExportQueryCondition"/>
    </select>

    <select id="countByMaxId" resultType="int">
        SELECT COUNT(*)
        FROM student_record
        WHERE <include refid="CurrentStudentVersionCondition"/>
          AND id &lt;= #{maxId}
    </select>

    <select id="countByMaxIdAndQuery" resultType="int">
        SELECT COUNT(*)
        FROM student_record
        WHERE <include refid="CurrentStudentVersionCondition"/>
          AND id &lt;= #{maxId}
        <include refid="StudentExportQueryCondition"/>
    </select>

    <select id="countByVersionAndMaxIdAndQuery" resultType="int">
        SELECT COUNT(*)
        FROM student_record
        WHERE import_version = #{version}
          AND id &lt;= #{maxId}
        <include refid="StudentExportQueryCondition"/>
    </select>

    <select id="listByCursor" resultType="com.huang.demo.excel.domain.model.StudentExportRecord">
        SELECT id, student_no, name, age, gender, class_name, email, birthday
        FROM student_record
        WHERE <include refid="CurrentStudentVersionCondition"/>
          AND id &gt; #{lastId}
          AND id &lt;= #{maxId}
        ORDER BY id
        LIMIT #{limit}
    </select>

    <select id="listByCursorAndQuery" resultType="com.huang.demo.excel.domain.model.StudentExportRecord">
        SELECT id, student_no, name, age, gender, class_name, email, birthday
        FROM student_record
        WHERE <include refid="CurrentStudentVersionCondition"/>
          AND id &gt; #{lastId}
          AND id &lt;= #{maxId}
        <include refid="StudentExportQueryCondition"/>
        ORDER BY id
        LIMIT #{limit}
    </select>

    <select id="listByCursorAndVersionAndQuery" resultType="com.huang.demo.excel.domain.model.StudentExportRecord">
        SELECT id, student_no, name, age, gender, class_name, email, birthday
        FROM student_record
        WHERE import_version = #{version}
          AND id &gt; #{lastId}
          AND id &lt;= #{maxId}
        <include refid="StudentExportQueryCondition"/>
        ORDER BY id
        LIMIT #{limit}
    </select>

    <insert id="saveBatch">
        INSERT INTO student_record (import_version, student_no, name, age, gender, class_name, email, birthday)
        VALUES
        <foreach collection="rows" item="row" separator=",">
            ((SELECT current_version FROM student_import_version_control WHERE id = 1),
             #{row.studentNo}, #{row.name}, #{row.age}, #{row.gender}, #{row.className}, #{row.email}, #{row.birthday})
        </foreach>
        ON DUPLICATE KEY UPDATE
            name = VALUES(name),
            age = VALUES(age),
            gender = VALUES(gender),
            class_name = VALUES(class_name),
            email = VALUES(email),
            birthday = VALUES(birthday)
    </insert>

    <insert id="saveImportStageBatch">
        INSERT INTO student_import_stage
            (import_task_id, row_no, student_no, name, age, gender, class_name, email, birthday)
        VALUES
        <foreach collection="rows" item="row" separator=",">
            (#{row.importTaskId}, #{row.rowNo}, #{row.studentNo}, #{row.name}, #{row.age},
             #{row.gender}, #{row.className}, #{row.email}, #{row.birthday})
        </foreach>
    </insert>

    <select id="countImportStageRows" resultType="int">
        SELECT COUNT(*)
        FROM student_import_stage
        WHERE import_task_id = #{importTaskId}
    </select>

    <select id="countInvalidImportStageRows" resultType="int">
        SELECT COUNT(*)
        FROM student_import_stage
        WHERE import_task_id = #{importTaskId}
          AND (
              student_no IS NULL OR TRIM(student_no) = ''
              OR CHAR_LENGTH(student_no) &gt; 32
              OR name IS NULL OR TRIM(name) = ''
              OR CHAR_LENGTH(name) &gt; 64
              OR age &lt; 0 OR age &gt; 150
              OR CHAR_LENGTH(gender) &gt; 16
              OR CHAR_LENGTH(class_name) &gt; 64
              OR CHAR_LENGTH(email) &gt; 128
              OR CHAR_LENGTH(birthday) &gt; 32
              OR (email IS NOT NULL AND TRIM(email) &lt;&gt; '' AND email NOT LIKE '%_@_%._%')
          )
    </select>

    <select id="countDuplicateImportStageStudentNo" resultType="int">
        SELECT COUNT(*)
        FROM (
            SELECT student_no
            FROM student_import_stage
            WHERE import_task_id = #{importTaskId}
              AND student_no IS NOT NULL
              AND TRIM(student_no) &lt;&gt; ''
            GROUP BY student_no
            HAVING COUNT(*) &gt; 1
        ) duplicate_student_no
    </select>

    <select id="listInvalidImportStageRows" resultType="com.huang.demo.excel.domain.model.StudentImportStageRecord">
        SELECT import_task_id, row_no, student_no, name, age, gender, class_name, email, birthday
        FROM student_import_stage
        WHERE import_task_id = #{importTaskId}
          AND (
              student_no IS NULL OR TRIM(student_no) = ''
              OR CHAR_LENGTH(student_no) &gt; 32
              OR name IS NULL OR TRIM(name) = ''
              OR CHAR_LENGTH(name) &gt; 64
              OR age &lt; 0 OR age &gt; 150
              OR CHAR_LENGTH(gender) &gt; 16
              OR CHAR_LENGTH(class_name) &gt; 64
              OR CHAR_LENGTH(email) &gt; 128
              OR CHAR_LENGTH(birthday) &gt; 32
              OR (email IS NOT NULL AND TRIM(email) &lt;&gt; '' AND email NOT LIKE '%_@_%._%')
          )
        ORDER BY row_no
    </select>

    <select id="listDuplicateImportStageStudentNoRows" resultType="com.huang.demo.excel.domain.model.StudentImportStageRecord">
        SELECT stage.import_task_id, stage.row_no, stage.student_no, stage.name, stage.age,
               stage.gender, stage.class_name, stage.email, stage.birthday
        FROM student_import_stage stage
        INNER JOIN (
            SELECT student_no
            FROM student_import_stage
            WHERE import_task_id = #{importTaskId}
              AND student_no IS NOT NULL
              AND TRIM(student_no) &lt;&gt; ''
            GROUP BY student_no
            HAVING COUNT(*) &gt; 1
        ) duplicate_student_no
            ON stage.student_no = duplicate_student_no.student_no
        WHERE stage.import_task_id = #{importTaskId}
        ORDER BY stage.student_no, stage.row_no
    </select>

    <insert id="mergeImportStageToStudent">
        INSERT INTO student_record (import_version, import_task_id, student_no, name, age, gender, class_name, email, birthday)
        SELECT (SELECT current_version FROM student_import_version_control WHERE id = 1),
               import_task_id, student_no, name, age, gender, class_name, email, birthday
        FROM student_import_stage
        WHERE import_task_id = #{importTaskId}
        ORDER BY row_no
        ON DUPLICATE KEY UPDATE
            name = VALUES(name),
            age = VALUES(age),
            gender = VALUES(gender),
            class_name = VALUES(class_name),
            email = VALUES(email),
            birthday = VALUES(birthday)
    </insert>

    <insert id="mergeImportStageRangeToStudent">
        INSERT INTO student_record (import_version, import_task_id, student_no, name, age, gender, class_name, email, birthday)
        SELECT #{importVersion}, import_task_id, student_no, name, age, gender, class_name, email, birthday
        FROM student_import_stage
        WHERE import_task_id = #{importTaskId}
          AND row_no &gt;= #{startRowNo}
          AND row_no &lt;= #{endRowNo}
        ORDER BY row_no
        ON DUPLICATE KEY UPDATE
            name = VALUES(name),
            age = VALUES(age),
            gender = VALUES(gender),
            class_name = VALUES(class_name),
            email = VALUES(email),
            birthday = VALUES(birthday)
    </insert>

    <delete id="deleteStudentRowsByImportTaskId">
        DELETE FROM student_record
        WHERE import_task_id = #{importTaskId}
          AND import_version &lt;&gt; (SELECT current_version FROM student_import_version_control WHERE id = 1)
    </delete>

    <delete id="deleteExpiredStudentVersions">
        DELETE FROM student_record
        WHERE import_version IN (
            SELECT import_version
            FROM (
                SELECT old_versions.import_version
                FROM (
                    SELECT import_version
                    FROM student_record
                    WHERE import_version &lt;&gt; (SELECT current_version FROM student_import_version_control WHERE id = 1)
                    GROUP BY import_version
                    ORDER BY import_version DESC
                    LIMIT 18446744073709551615 OFFSET #{retainCount}
                ) old_versions
            ) deletable_versions
        )
        ORDER BY id ASC
        LIMIT #{limit}
    </delete>

    <delete id="deleteImportStage">
        DELETE FROM student_import_stage
        WHERE import_task_id = #{importTaskId}
    </delete>

    <delete id="deleteImportStageBefore">
        DELETE FROM student_import_stage
        WHERE created_at &lt;= #{createdBefore}
        ORDER BY id ASC
        LIMIT #{limit}
    </delete>
</mapper>
