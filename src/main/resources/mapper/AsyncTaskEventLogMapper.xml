<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huang.demo.task.repository.AsyncTaskEventLogMapper">

    <resultMap id="AsyncTaskEventLogMap" type="com.huang.demo.task.domain.entity.AsyncTaskEventLog">
        <id property="id" column="id"/>
        <result property="eventId" column="event_id"/>
        <result property="taskId" column="task_id"/>
        <result property="ownerId" column="owner_id"/>
        <result property="taskType" column="task_type"/>
        <result property="eventType" column="event_type"/>
        <result property="message" column="message"/>
        <result property="progressPercent" column="progress_percent"/>
        <result property="completedCount" column="completed_count"/>
        <result property="totalCount" column="total_count"/>
        <result property="failureType" column="failure_type"/>
        <result property="traceId" column="trace_id"/>
        <result property="workerId" column="worker_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <update id="createTableIfAbsent">
        CREATE TABLE IF NOT EXISTS async_task_event_log (
            id BIGINT AUTO_INCREMENT PRIMARY KEY,
            event_id VARCHAR(64) NOT NULL,
            task_id VARCHAR(64) NOT NULL,
            owner_id VARCHAR(64) NOT NULL,
            task_type VARCHAR(32) NOT NULL,
            event_type VARCHAR(32) NOT NULL,
            message VARCHAR(1024),
            progress_percent INT,
            completed_count BIGINT,
            total_count BIGINT,
            failure_type VARCHAR(32),
            trace_id VARCHAR(64),
            worker_id VARCHAR(128),
            created_at DATETIME NOT NULL,
            UNIQUE KEY uk_async_task_event_log_event_id (event_id),
            KEY idx_async_task_event_log_task_created_at (task_id, created_at),
            KEY idx_async_task_event_log_owner_created_at (owner_id, created_at)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    </update>

    <insert id="insert" parameterType="com.huang.demo.task.domain.entity.AsyncTaskEventLog">
        INSERT INTO async_task_event_log (
            event_id, task_id, owner_id, task_type, event_type, message,
            progress_percent, completed_count, total_count, failure_type,
            trace_id, worker_id, created_at
        )
        VALUES (
            #{eventId}, #{taskId}, #{ownerId}, #{taskType}, #{eventType}, #{message},
            #{progressPercent}, #{completedCount}, #{totalCount}, #{failureType},
            #{traceId}, #{workerId}, #{createdAt}
        )
    </insert>

    <select id="listByTaskId" resultMap="AsyncTaskEventLogMap">
        SELECT id, event_id, task_id, owner_id, task_type, event_type, message,
               progress_percent, completed_count, total_count, failure_type,
               trace_id, worker_id, created_at
        FROM async_task_event_log
        WHERE task_id = #{taskId}
        ORDER BY id ASC
        LIMIT #{limit}
    </select>
</mapper>
