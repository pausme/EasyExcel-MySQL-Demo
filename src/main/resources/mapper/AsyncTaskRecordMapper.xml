<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huang.demo.task.repository.AsyncTaskRecordMapper">

    <resultMap id="AsyncTaskRecordMap" type="com.huang.demo.task.domain.entity.AsyncTaskRecord">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="ownerId" column="owner_id"/>
        <result property="taskType" column="task_type"/>
        <result property="taskName" column="task_name"/>
        <result property="businessKey" column="business_key"/>
        <result property="status" column="status"/>
        <result property="progressPercent" column="progress_percent"/>
        <result property="totalCount" column="total_count"/>
        <result property="completedCount" column="completed_count"/>
        <result property="retryCount" column="retry_count"/>
        <result property="maxRetryCount" column="max_retry_count"/>
        <result property="requestPayload" column="request_payload"/>
        <result property="resultPayload" column="result_payload"/>
        <result property="errorMessage" column="error_message"/>
        <result property="failureType" column="failure_type"/>
        <result property="retryable" column="retryable"/>
        <result property="failureSuggestion" column="failure_suggestion"/>
        <result property="traceId" column="trace_id"/>
        <result property="workerId" column="worker_id"/>
        <result property="lastHeartbeatAt" column="last_heartbeat_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="startedAt" column="started_at"/>
        <result property="finishedAt" column="finished_at"/>
        <result property="expireAt" column="expire_at"/>
    </resultMap>

    <sql id="BaseColumns">
        id, task_id, owner_id, task_type, task_name, business_key, status,
        progress_percent, total_count, completed_count, retry_count, max_retry_count,
        request_payload, result_payload, error_message, failure_type, retryable, failure_suggestion, trace_id,
        worker_id, last_heartbeat_at, created_at, updated_at,
        started_at, finished_at, expire_at
    </sql>

    <update id="createTableIfAbsent">
        CREATE TABLE IF NOT EXISTS async_task_record (
            id BIGINT AUTO_INCREMENT PRIMARY KEY,
            task_id VARCHAR(64) NOT NULL,
            owner_id VARCHAR(64) NOT NULL,
            task_type VARCHAR(32) NOT NULL,
            task_name VARCHAR(128) NOT NULL,
            business_key VARCHAR(128),
            status VARCHAR(32) NOT NULL,
            progress_percent INT NOT NULL,
            total_count BIGINT NOT NULL,
            completed_count BIGINT NOT NULL,
            retry_count INT NOT NULL,
            max_retry_count INT NOT NULL,
            request_payload TEXT,
            result_payload TEXT,
            error_message VARCHAR(1024),
            failure_type VARCHAR(32),
            retryable TINYINT(1),
            failure_suggestion VARCHAR(512),
            trace_id VARCHAR(64),
            worker_id VARCHAR(128),
            last_heartbeat_at DATETIME,
            created_at DATETIME NOT NULL,
            updated_at DATETIME NOT NULL,
            started_at DATETIME,
            finished_at DATETIME,
            expire_at DATETIME NOT NULL,
            UNIQUE KEY uk_async_task_record_task_id (task_id),
            KEY idx_async_task_record_owner_created_at (owner_id, created_at),
            KEY idx_async_task_record_owner_business_key_created_at (owner_id, business_key, created_at),
            KEY idx_async_task_record_owner_status_created_at (owner_id, status, created_at),
            KEY idx_async_task_record_type_status_created_at (task_type, status, created_at),
            KEY idx_async_task_record_status_heartbeat (status, last_heartbeat_at),
            KEY idx_async_task_record_expire_at (expire_at)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    </update>

    <insert id="insert" parameterType="com.huang.demo.task.domain.entity.AsyncTaskRecord">
        INSERT INTO async_task_record (
            task_id, owner_id, task_type, task_name, business_key, status,
            progress_percent, total_count, completed_count, retry_count, max_retry_count,
            request_payload, result_payload, error_message, failure_type, retryable, failure_suggestion, trace_id,
            worker_id, last_heartbeat_at, created_at, updated_at,
            started_at, finished_at, expire_at
        )
        VALUES (
            #{taskId}, #{ownerId}, #{taskType}, #{taskName}, #{businessKey}, #{status},
            #{progressPercent}, #{totalCount}, #{completedCount}, #{retryCount}, #{maxRetryCount},
            #{requestPayload}, #{resultPayload}, #{errorMessage}, #{failureType}, #{retryable}, #{failureSuggestion}, #{traceId},
            #{workerId}, #{lastHeartbeatAt}, #{createdAt}, #{updatedAt},
            #{startedAt}, #{finishedAt}, #{expireAt}
        )
    </insert>

    <update id="update" parameterType="com.huang.demo.task.domain.entity.AsyncTaskRecord">
        UPDATE async_task_record
        SET owner_id = #{ownerId},
            task_type = #{taskType},
            task_name = #{taskName},
            business_key = #{businessKey},
            status = #{status},
            progress_percent = #{progressPercent},
            total_count = #{totalCount},
            completed_count = #{completedCount},
            retry_count = #{retryCount},
            max_retry_count = #{maxRetryCount},
            request_payload = #{requestPayload},
            result_payload = #{resultPayload},
            error_message = #{errorMessage},
            failure_type = #{failureType},
            retryable = #{retryable},
            failure_suggestion = #{failureSuggestion},
            trace_id = #{traceId},
            worker_id = #{workerId},
            last_heartbeat_at = #{lastHeartbeatAt},
            updated_at = #{updatedAt},
            started_at = #{startedAt},
            finished_at = #{finishedAt},
            expire_at = #{expireAt}
        WHERE task_id = #{taskId}
    </update>

    <update id="claimRunning">
        UPDATE async_task_record
        SET status = 'RUNNING',
            failure_type = NULL,
            retryable = NULL,
            failure_suggestion = NULL,
            worker_id = #{workerId},
            last_heartbeat_at = #{heartbeatAt},
            updated_at = #{heartbeatAt},
            started_at = COALESCE(started_at, #{heartbeatAt})
        WHERE task_id = #{taskId}
          AND status = 'CREATED'
          AND expire_at > #{heartbeatAt}
    </update>

    <select id="findByTaskId" resultMap="AsyncTaskRecordMap">
        SELECT <include refid="BaseColumns"/>
        FROM async_task_record
        WHERE task_id = #{taskId}
        LIMIT 1
    </select>

    <select id="countActive" resultType="long">
        SELECT COUNT(*)
        FROM async_task_record
        WHERE status IN ('CREATED', 'RUNNING')
    </select>

    <select id="countActiveByOwner" resultType="long">
        SELECT COUNT(*)
        FROM async_task_record
        WHERE owner_id = #{ownerId}
          AND status IN ('CREATED', 'RUNNING')
    </select>

    <select id="countCreatedAtOrAfter" resultType="long">
        SELECT COUNT(*)
        FROM async_task_record
        WHERE created_at &gt;= #{createdAt}
    </select>

    <select id="countByStatusCreatedAtOrAfter" resultType="long">
        SELECT COUNT(*)
        FROM async_task_record
        WHERE status = #{status}
          AND created_at &gt;= #{createdAt}
    </select>

    <select id="countByStatuses" resultType="long">
        SELECT COUNT(*)
        FROM async_task_record
        WHERE status IN
        <foreach collection="statuses" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
    </select>

    <select id="listRecentByStatuses" resultMap="AsyncTaskRecordMap">
        SELECT <include refid="BaseColumns"/>
        FROM async_task_record
        WHERE status IN
        <foreach collection="statuses" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <select id="countByOwner" resultType="long">
        SELECT COUNT(*)
        FROM async_task_record
        WHERE owner_id = #{ownerId}
        <if test="taskTypes != null and taskTypes.size() > 0">
            AND task_type IN
            <foreach collection="taskTypes" item="taskType" open="(" separator="," close=")">
                #{taskType}
            </foreach>
        </if>
        <if test="statuses != null and statuses.size() > 0">
            AND status IN
            <foreach collection="statuses" item="status" open="(" separator="," close=")">
                #{status}
            </foreach>
        </if>
        <if test="businessKey != null and businessKey != ''">
            AND business_key = #{businessKey}
        </if>
        <if test="failureType != null and failureType != ''">
            AND failure_type = #{failureType}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (
                task_id LIKE CONCAT('%', #{keyword}, '%')
                OR task_name LIKE CONCAT('%', #{keyword}, '%')
                OR business_key LIKE CONCAT('%', #{keyword}, '%')
                OR error_message LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="createdFrom != null">
            AND created_at &gt;= #{createdFrom}
        </if>
        <if test="createdTo != null">
            AND created_at &lt;= #{createdTo}
        </if>
        <if test="minProgress != null">
            AND progress_percent &gt;= #{minProgress}
        </if>
        <if test="maxProgress != null">
            AND progress_percent &lt;= #{maxProgress}
        </if>
        <if test="retryable != null">
            AND retryable = #{retryable}
        </if>
    </select>

    <select id="listByOwnerPage" resultMap="AsyncTaskRecordMap">
        SELECT <include refid="BaseColumns"/>
        FROM async_task_record
        WHERE owner_id = #{ownerId}
        <if test="taskTypes != null and taskTypes.size() > 0">
            AND task_type IN
            <foreach collection="taskTypes" item="taskType" open="(" separator="," close=")">
                #{taskType}
            </foreach>
        </if>
        <if test="statuses != null and statuses.size() > 0">
            AND status IN
            <foreach collection="statuses" item="status" open="(" separator="," close=")">
                #{status}
            </foreach>
        </if>
        <if test="businessKey != null and businessKey != ''">
            AND business_key = #{businessKey}
        </if>
        <if test="failureType != null and failureType != ''">
            AND failure_type = #{failureType}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (
                task_id LIKE CONCAT('%', #{keyword}, '%')
                OR task_name LIKE CONCAT('%', #{keyword}, '%')
                OR business_key LIKE CONCAT('%', #{keyword}, '%')
                OR error_message LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="createdFrom != null">
            AND created_at &gt;= #{createdFrom}
        </if>
        <if test="createdTo != null">
            AND created_at &lt;= #{createdTo}
        </if>
        <if test="minProgress != null">
            AND progress_percent &gt;= #{minProgress}
        </if>
        <if test="maxProgress != null">
            AND progress_percent &lt;= #{maxProgress}
        </if>
        <if test="retryable != null">
            AND retryable = #{retryable}
        </if>
        ORDER BY ${orderBy}
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByOwnerAndBusinessKey" resultType="long">
        SELECT COUNT(*)
        FROM async_task_record
        WHERE owner_id = #{ownerId}
          AND business_key = #{businessKey}
        <if test="taskType != null and taskType != ''">
            AND task_type = #{taskType}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
    </select>

    <select id="listByOwnerAndBusinessKeyPage" resultMap="AsyncTaskRecordMap">
        SELECT <include refid="BaseColumns"/>
        FROM async_task_record
        WHERE owner_id = #{ownerId}
          AND business_key = #{businessKey}
        <if test="taskType != null and taskType != ''">
            AND task_type = #{taskType}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="markExpiredBefore">
        UPDATE async_task_record
        SET status = 'EXPIRED',
            updated_at = NOW(),
            finished_at = NOW()
        WHERE expire_at &lt;= #{expireBefore}
          AND status IN ('CREATED', 'RUNNING')
    </update>

    <update id="updateHeartbeat">
        UPDATE async_task_record
        SET worker_id = #{workerId},
            last_heartbeat_at = #{heartbeatAt},
            updated_at = #{heartbeatAt}
        WHERE task_id = #{taskId}
          AND status = 'RUNNING'
    </update>

    <select id="listRecoverable" resultMap="AsyncTaskRecordMap">
        SELECT <include refid="BaseColumns"/>
        FROM async_task_record
        WHERE (
                status = 'CREATED'
                AND created_at &lt;= #{heartbeatBefore}
              )
           OR (
                status = 'RUNNING'
                AND (last_heartbeat_at IS NULL OR last_heartbeat_at &lt;= #{heartbeatBefore})
              )
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

    <select id="listByTypeAndStatusAfterId" resultMap="AsyncTaskRecordMap">
        SELECT <include refid="BaseColumns"/>
        FROM async_task_record
        WHERE task_type = #{taskType}
          AND status = #{status}
          AND id &gt; #{lastId}
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

    <update id="claimRecoverable">
        UPDATE async_task_record
        SET status = 'CREATED',
            worker_id = #{workerId},
            last_heartbeat_at = NOW(),
            updated_at = NOW(),
            error_message = NULL,
            failure_type = NULL,
            retryable = NULL,
            failure_suggestion = NULL
        WHERE task_id = #{taskId}
          AND (
                (
                    status = 'CREATED'
                    AND created_at &lt;= #{heartbeatBefore}
                )
                OR (
                    status = 'RUNNING'
                    AND (last_heartbeat_at IS NULL OR last_heartbeat_at &lt;= #{heartbeatBefore})
                )
          )
    </update>

    <delete id="deleteTerminalBefore">
        DELETE FROM async_task_record
        WHERE status IN ('SUCCESS', 'FAILED', 'CANCELED', 'EXPIRED')
          AND updated_at &lt;= #{updatedBefore}
        ORDER BY id ASC
        LIMIT #{limit}
    </delete>
</mapper>
