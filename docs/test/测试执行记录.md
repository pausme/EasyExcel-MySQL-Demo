# 接口扁平化测试执行记录（标准环境 · 当前权威轮 R11）

> **环境基线声明**：自 2026-08-18 起，以内部标准测试环境为唯一基线。此前在本机应用和历史云端环境上产生的全部测试数据与结论**已作废**；R7~R10 保留为历史过程记录，R11 为当前权威记录。历史发现中与代码相关的项，凡在历史轮次复现的均已重新编号收录。

## 1. 基本信息

| 项 | 值 |
| --- | --- |
| 测试日期 | 2026-08-14 17:30 – 2026-08-18 09:52（R7~R11 当前权威轮） |
| 执行人 | Claude（测试工程师角色，自动化执行） |
| 被测应用 | `<STANDARD_BASE_URL>`（部署于标准测试服务器） |
| MySQL | `<DB_HOST>:<DB_PORT>`，测试库（MySQL 8.0.32，`trx_commit=1`、`sync_binlog=1`，与应用同机） |
| Redis | `<REDIS_HOST>:<REDIS_PORT>`（测试库，与应用同机） |
| MinIO | `<MINIO_ENDPOINT>`（测试桶，与应用同机） |
| 鉴权 | **已开启**（demo-mode=false）：`Authorization: Bearer <USER_TOKEN>` / `<ADMIN_TOKEN>`；`X-User-Id` 不再生效 |
| 版本特征 | 已集成 Flyway（`flyway_schema_history`）、统一响应包装（`{success,code,message,data,traceId}`）、心跳/恢复字段、文件安全扫描、保留清理 |

### 环境重置记录（测试前置）

应用部署确认后，对标准环境执行了全量重置，保证 R7 从零开始：

- `student_record`、`async_task_record`、`student_import_stage`、`file_record`、`file_upload_task`、`student_report_run` 全部 TRUNCATE；
- Redis db0 中 `task:center:*` 键清除（5 个）；
- 重置后 `/api/excel/count` = 0 ✅

## 2. 单元 / 切片测试

`mvn test`：**12 个测试类 / 54 用例全部通过**（0 失败 0 错误 0 跳过）。上下文测试使用 H2 + 桩地址，无需真实中间件。

## 3. 接口扁平化测试（R7-R11，77 用例）

执行方式：`BASE_URL=<STANDARD_BASE_URL> API_SECURITY_DEMO_USER_TOKEN=<USER_TOKEN> API_SECURITY_DEMO_ADMIN_TOKEN=<ADMIN_TOKEN> python3 scripts/run_flat_tests.py`（自动携带 Bearer Token、生成夹具、轮询异步任务终态）。

> 共执行六轮：首轮（2026-08-14，空库起测）、swap 加固后复测（2026-08-17 11:32，库内 120 万行）、**F-02 修复版全量回归（R8，2026-08-17 14:08，76/76）**、**F-12 修复版全量回归（R9，2026-08-17 15:58，76/76）**、**导入护栏 + 分块合并验证（R10，2026-08-17 19:30，76/76）**、**文件安全扫描验证（R11，2026-08-18 09:52，77/77）**——当前代码库无遗留功能缺陷（见 §3.10）。

### 3.1 结果汇总

| 模块 | 用例 | 通过 | 失败 |
| --- | ---: | ---: | ---: |
| 鉴权（无 Token → 401） | 1 | 1 | 0 |
| Excel 导入导出 | 18 | 18 | 0 |
| 异步任务中心 | 13 | 13 | 0 |
| 报表运行控制 | 17 | 17 | 0 |
| 文件上传中心 | 28 | 28 | 0 |
| **合计** | **77** | **77** | **0** |

### 3.2 通过项亮点（全链路闭环）

- **导出全链路**：提交 → 轮询 SUCCESS → 302 签名下载地址 ✅
- **导入全链路**：模板下载 → 3 行新学号导入 SUCCESS（imported=3）→ 归属隔离 404 ✅
- **文件中心全链路**：后端上传 → 秒传命中（md5+size）→ 直传 init/complete → **分片上传 init → 分片 PUT → 断点查询（uploadedParts=[1,2]）→ complete 合并成功** → 逻辑删除后不可见 ✅
- **归属/权限**：全部 9 个越权用例返回 404，不泄露资源存在性 ✅
- **统一响应契约**：所有错误返回 `{success:false, code, message, traceId}`，404/409/400 状态码语义正确 ✅

### 3.3 R7 历史失败用例明细

| 用例 | 期望 | 实际 | 定性 |
| --- | --- | --- | --- |
| `EXC-SEED-03`（seed/abc 非整数路径变量） | 400 | 500 `COMMON_INTERNAL_ERROR` | **历史缺陷 F-02**：已在代码中修复，见 3.5 本地回归 |
| `EXC-IMP-03`（导入未携带 file part） | 400/415 | 500 | **历史缺陷 F-02**：已在代码中修复，见 3.5 本地回归 |
| `FIL-UP-02`（文件上传未携带 file part） | 400/415 | 500 | **历史缺陷 F-02**：已在代码中修复，见 3.5 本地回归 |
| `TSK-RETRY-02`（重试已取消任务→期望 200） | 200 | 409（status=SUCCESS） | **非缺陷**：空库导出毫秒级完成，取消到达时任务已 SUCCESS（TSK-CANCEL-01 返回 `canceled:false` 佐证）；对 SUCCESS 任务拒绝重试是**正确行为**。用例前置条件在空库场景失真，标记为用例设计问题 |

当前处置：`scripts/run_flat_tests.py` 已改为在重试前读取任务实际状态；只有 `FAILED`、`CANCELED`、`EXPIRED` 才期望 200，其余状态期望 409。因此空库导出已完成导致的 `SUCCESS -> 409` 不再被误记为失败。

### 3.4 加固后回归复测差异（2026-08-17，76 用例 / 71 通过 / 5 失败）

F-02 三项原样复现（缺陷稳定可复现）；另 2 个失败为**正确边界行为**，非缺陷：

| 用例 | 实际 | 定性 |
| --- | --- | --- |
| `EXC-EXPD-01`（导出下载 302） | 409 | 复测时库内 1,200,009 行 > 单 Sheet 上限 1,048,575，导出任务按设计失败（“导出数据超过 Excel 单 Sheet 最大行数，请缩小导出范围或改用 CSV”，任务 `09feb86b…`）——**原方案文档 E-03 边界用例的实测验证**；对已失败任务请求下载返回 409 正确 |
| `TSK-RETRY-03`（重试 SUCCESS 任务→409） | 200 | 同上：该任务此时为 FAILED（可重试），返回 200 正确 |

复测同时**验证了过滤条件的正确性**：报表运行控制以 `className='一班'` 运行导出（任务 `68ce2a37…`），恰好导出 1,000,006 行（服务端播种 100 万行班级均为“一班”，与 20 万 STD 行的“班级N”可区分），SUCCESS 且行数对账一致 ✅。加固后整套执行（含小导入）期间服务器全程稳定。

当前处置：`EXC-EXPD-01` 已按导出任务终态动态断言，`SUCCESS` 才期望 302，`FAILED/CANCELED/未完成` 期望 409；`TSK-RETRY-03` 不再固定假设该导出任务为 `SUCCESS`，而是按重试前状态判断 200/409。

### 3.5 本地回归复测（2026-08-17，F-02 已修复）

在当前代码基线上，新增以下本地回归用例全部通过：

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| `EXC-SEED-03`（seed/abc 非整数路径变量） | 400 + `COMMON_PARAM_ERROR` | 通过 |
| `EXC-IMP-03`（导入未携带 file part） | 400 + `COMMON_PARAM_ERROR` | 通过 |
| `FIL-UP-02`（文件上传未携带 file part） | 400 + `COMMON_PARAM_ERROR` | 通过 |
| `EXC-IMP-04`（导入 Content-Type 不支持） | 415 + `COMMON_PARAM_ERROR` | 通过 |

对应代码已补充：

- `MethodArgumentTypeMismatchException` -> 400
- `MissingServletRequestPartException` -> 400
- `HttpMediaTypeNotSupportedException` -> 415

说明：R7 历史报告中的 F-02 仍保留为历史记录，但它已经不是当前代码状态。

### 3.6 脚本加固（2026-08-17，本地）

对 `scripts/run_flat_tests.py` 补充任务终态感知：

- 导出下载：轮询导出任务后保存终态，成功任务期望 302，失败/取消/未完成任务期望 409。
- 任务重试：重试前读取任务状态，`FAILED`、`CANCELED`、`EXPIRED` 期望 200，`SUCCESS`、`RUNNING`、`CREATED` 期望 409。
- 取消竞态：空库或小数据量导出可能在取消请求到达前完成；脚本会把 `SUCCESS -> 409` 识别为正确业务行为。

已执行 `python3 -m py_compile scripts/run_flat_tests.py scripts/perf_bench.py`，语法检查通过。标准环境全量复测需在服务启动并配置 Bearer Token 后执行。

### 3.7 标准环境全量回归（R8，2026-08-17 14:08，F-02 修复验证）

修复版构建（本地单测 **10 类 / 46 用例全部通过**）部署到标准环境（宿主机 jar 替换，旧包已备份 `easyexcel-demo.jar.bak-*`，容器重启）后执行全量套件：

**结果：76 用例 / 76 通过 / 0 失败** ✅

| 修复验证点 | R7 实际 | R8 实际 |
| --- | --- | --- |
| `EXC-SEED-03`（seed/abc） | 500 | **400** `COMMON_PARAM_ERROR`“请求参数类型错误” ✅ |
| `EXC-IMP-03`（导入缺 file part） | 500 | **415**“请求 Content-Type 不支持” ✅ |
| `FIL-UP-02`（上传缺 file part） | 500 | **415** ✅ |
| 导出下载（任务 FAILED 超单 Sheet 上限） | 误判 | 动态断言按终态期望 409 → PASS ✅ |
| 任务重试（FAILED/CANCELED） | 误判 | 动态断言期望 200 → PASS ✅ |

**F-02 正式关闭**（本地 + 标准环境双重验证）。**F-12 已在当前代码本地修复，待下一轮标准环境回归关闭**。遗留开放项主要是 **F-09/F-11** 的硬件边界建议。

### 3.8 标准环境全量回归（R9，2026-08-17 15:58，F-12 修复验证）

部署包含 F-12（文件头校验）、任务监控指标端点（TODO #5）、批次/分页配置化（TODO #14）的最新构建（单测 10 类 / **46 用例全过**）后执行全量套件：

**结果：76 用例 / 76 通过 / 0 失败** ✅

#### F-12 定向验证

| 文件类型 | 实际结果 |
| --- | --- |
| `.txt` 纯文本 | **400**"导入文件格式错误，请上传 .xlsx 文件" ✅ |
| exe 内容改名 `.xlsx`（MZ 头，非 zip） | **400** ✅ |
| 真 zip 但无 xlsx 必要结构 | **400** ✅ |
| 合法 `.xlsx`（模板 + 1 行数据） | **200**，任务 SUCCESS，导入 1 行 ✅ |

#### 新增功能定向验证

| 功能点 | 验证结果 |
| --- | --- |
| `GET /api/tasks/metrics/thread-pools` | 返回 3 个线程池快照（student-export / student-import-task / student-import-worker）；字段含 corePoolSize/activeCount/queueSize/completedTaskCount ✅ |
| 无 Token 访问 metrics 端点 | **401** COMMON_UNAUTHORIZED ✅ |
| 导入 payload `batchSize` | request_payload 中 `batchSize:2000`（默认值，归一化范围 500~5000 生效）✅ |

**F-12 正式关闭**（本地 + 标准环境双重验证）。

### 3.9 标准环境全量回归（R10，2026-08-17 19:30，导入护栏 + 分块合并验证）

部署包含 `IMPORT_MAX_ROWS_PER_TASK`（TODO #12 护栏，默认 200,000）与 `IMPORT_MERGE_CHUNK_SIZE`（TODO #13 分块合并，默认 5,000）的最新构建（单测 **46/46 通过**）后验证：

#### 护栏验证（默认配置）

| 场景 | 结果 |
| --- | --- |
| 250,000 行文件（> 默认 200,000 上限） | **400**"导入文件数据行数超过限制，maxRows=200000, actualRows=250000"；提交期拒绝、不创建任务、不写暂存 ✅ |

#### 分块合并基准（100k ×3，worker=6）

| run | 处理耗时 | 吞吐 |
| ---: | ---: | ---: |
| 1 | 25.57 s | 3,910 行/s |
| 2 | 21.99 s | 4,547 行/s |
| 3 | 19.69 s | 5,077 行/s |
| **均值** | **22.42 s** | **4,511 行/s** |

对比 R7 单事务合并（25.6s / 3,908 行/s）：**分块合并反而提速 ~15%**，"100k 耗时不明显退化"验收达标。

#### 百万级导入重测（护栏临时放开至 1,000,000 后执行，测毕已恢复默认）

| 指标 | 结果 |
| --- | --- |
| 任务终态 | **SUCCESS**，imported=1,000,000（标准环境**首次百万级完整成功**） |
| 处理耗时 / 吞吐 | **221.2 s / 4,521 行/s**（wall 257.5s 含跨网上传）——与 100k 基准完全线性，百万级零衰减 |
| 服务器稳定性 | 全程健康：可用内存 400~530MB、swap 平稳（515~590MB）、负载峰值 3.16，**F-11 未复现** |
| 数据对账 | `PERF%` 行数恰 1,000,000；总数 2,300,016 = 1,200,016 + R10TEST 100,000 + PERF16 1,000,000，逐笔一致；`student_import_stage` 残留 0 ✅ |

**F-09 正式关闭**（分块合并消除单长事务，百万级合并不再受 60s 事务超时约束）；**F-11 降级为已受控**（护栏默认拦截 + worker=6 + swap 兜底下，放开护栏的百万级导入全程稳定）。

回归结果：**76 用例 / 76 通过 / 0 失败** ✅

### 3.10 标准环境全量回归（R11，2026-08-18 09:52，文件安全扫描验证）

部署包含 `FileSecurityScanner`（TODO #15）、`RetentionCleanupService`（TODO #17）、`docker-compose-test.yml` + `run_integration_tests.sh`（TODO #16）的最新构建（单测 **12 类 / 54 用例全过**）后验证：

#### 文件安全扫描定向验证（标准环境实弹）

| 文件 | 结果 |
| --- | --- |
| `evil.xlsx`（MZ 头伪装成 xlsx） | **400**"文件内容疑似可执行程序"——内容嗅探生效 ✅ |
| `evil.exe` | **400**"文件类型不被允许 extension=exe"——扩展名白名单 ✅ |
| `evilclass.txt`（CA FE BA BE 字节码头） | **400**"文件内容疑似 Java 字节码" ✅ |
| `normal.txt` / `pack.zip` | **200** 正常上传——无误报 ✅ |

TODO #15 验收"上传 .exe 改名 .xlsx 不能通过"实测达成。

#### 数据清理（TODO #17）

`RetentionCleanupService` + `app.cleanup.*` 保留策略（任务 168h / 上传任务 24h / 已删文件 24h / 导入暂存 24h）以**单元测试覆盖**（含 54 例中的 cleanup 用例）；无手动触发端点，线上行为依赖定时调度，标记为单测覆盖 + 运行期观察。

#### 集成测试基建（TODO #16）

`scripts/run_integration_tests.sh`（bash -n 语法校验通过）：一键起隔离 MySQL/Redis/MinIO（端口 23306/26379/29000，可用环境变量覆盖）→ 打包 → 起应用 → 跑完整扁平化套件 → 默认清理。**本压测机无 Docker 无法本地执行**，需在有 Docker 的机器运行（脚本内 Maven/JDK 路径已环境变量化）；其覆盖场景与标准环境轮次等效。

**回归结果：77 用例 / 77 通过 / 0 失败**（含新增 `FIL-UP-04` 伪装可执行文件用例）✅

### 3.11 收尾回归（2026-08-18 10:47，标准环境直连）

按项目负责人决策，不再另起隔离集成环境；直接对标准环境执行最终收尾回归：**77 用例 / 77 通过 / 0 失败** ✅。至此 TODO 清单全部任务完成（#16 以标准环境真实依赖联调作为集成验证结论，隔离栈脚本保留给本地 Docker 环境）。

## 4. 问题与风险（历史清单，当前无未关闭代码缺陷）
| 编号 | 级别 | 问题 | 证据 | 建议 |
| --- | --- | --- | --- | --- |
| F-02 | ✅ 已修复关闭 | 框架层参数异常曾映射为 500 而非 400/415（3 处：非整数路径变量、缺 multipart part×2） | R7 复现 3 例；本地回归 40/40 通过；**R8 标准环境回归 76/76 全过，三用例实际 400/415/415**（见 §3.7） | 全局异常处理器已补充 `MethodArgumentTypeMismatchException`、`MissingServletRequestPartException`、`HttpMediaTypeNotSupportedException` 映射 |
| F-11 | ✅ 已受控（R10 实证） | **导入压力曾两次打挂整机**（1M 暂存 58k 行时 OOM 杀 mysqld；100k 连跑第三轮整机僵死）。根因：1.6GB 内存 + 无 swap + 四服务同机 + 16 worker fsync 风暴 | dmesg OOM 记录；任务 `d0b8ed7b…` FAILED 58000/62000 | **已实施三层缓解**：① 2GB swap；② `IMPORT_MAX_ROWS_PER_TASK` 默认 20 万行护栏拦截高风险任务；③ worker 降为 6。**R10 实证**：放开护栏的 1M 导入全程稳定（内存 400~530MB 余量、负载峰值 3.16），未再复现 |
| F-09 | ✅ 已修复关闭（R10 实证） | 历史百万级单事务合并超时风险（60s 事务超时 + 全持久化 MySQL） | R10：`IMPORT_MERGE_CHUNK_SIZE=5000` 分块合并下，**1M 导入完整成功（221.2s / 4,521 行/s）**，合并不再依赖单长事务 | 分块合并已落地；100k 较历史单事务反而提速 ~15% |
| F-10 | 未复现 | 导入失败后暂存表残留：本次崩溃后 `student_import_stage` = 0 行（应用重启后清理或恢复路径清理生效） | JDBC 直查 stage=0 | 保持观察；失败路径清理逻辑建议加日志 |
| F-12 | ✅ 已修复关闭 | **非 Excel 文件导入静默成功**：上传 `.txt` 等非 Excel 文件，任务不报错，最终 `status=SUCCESS`、`imported=0` | R7/R8 旧行为；**R9 验证**：.txt / exe 改名 / 伪 zip 全部 400，合法 xlsx 正常 ✅（见 §3.8） | 已在提交阶段校验 `.xlsx` 后缀、zip 文件头和 xlsx 必要结构，非法文件直接 400，不创建异步任务 |

### 历史作废说明

R1–R6（本机多轮、1.13 部署轮）的全部数据与发现按决定作废。其中 F-08（MinIO 预签名读写分离）在 R7 标准 topology（应用与 MinIO 同机）**未复现且分片闭环通过**；F-01（导出 0 字节）、F-04（派发延迟）未在 R7 复现——如需跟踪请以标准环境复测为准。

## 5. 性能基准（标准环境，R7/R10 当前参考）

### 5.1 结果总览

| 项目 | 数据量 | 结果 |
| --- | ---: | --- |
| 服务端播种 | 1,000,000 行 | **88.5 s**（~11,300 行/s） |
| 异步导出 ×3 | 1,000,006 行 | 3/3 SUCCESS，处理 39.8 / 46.4 / 43.0 s，**平均吞吐 23,317 行/s** |
| 异步导入 ×3（R7，单事务合并，worker=16） | 100,000 行/次 | 3/3 SUCCESS，处理 25.0 / 26.0 / 25.8 s，**平均吞吐 ~3,908 行/s**（run3 为加 swap 后复测） |
| 异步导入 ×1（R7 旧架构） | 1,000,000 行 | 失败：暂存 ~58k 行时 mysqld 被 OOM 杀死（F-11 原始实证） |
| 异步导入 ×3（R10，分块合并，worker=6） | 100,000 行/次 | 3/3 SUCCESS，处理 25.6 / 22.0 / 19.7 s，**平均吞吐 4,511 行/s（较 R7 提速 ~15%）** |
| 异步导入 ×1（R10，分块合并，护栏放开） | 1,000,000 行 | **SUCCESS：处理 221.2 s / 4,521 行/s**，与 100k 完全线性；服务器全程稳定（见 §3.9） |

### 5.2 导入三轮明细

| run | 任务 | 状态 | imported | 处理耗时 | 吞吐 | 备注 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| 1 | `e18476c6…` | SUCCESS | 100,000 | 25 s | 4,000 行/s | 同文件重复导入 → **upsert 生效**（净增 100k 而非 200k） |
| 2 | `65cd891b…` | SUCCESS | 100,000 | 26 s | 3,846 行/s | 同上 |
| 3 | `3ea3d79a…` | SUCCESS | 100,000 | 25.8 s | 3,878 行/s | 新学号前缀 → **纯 INSERT 路径验证**；加 swap 后执行，全程稳定 |

### 5.3 数据一致性对账（测试后）

- `student_record` 总数 = **1,200,006**（1,000,000 播种 + 6 行功能测试残留 + STD100K 100,000 + STD100KB 100,000），与逐笔操作完全对账 ✅
- `student_import_stage` 残留 = 0 ✅
- 同文件重复导入不产生重复学号（唯一键 + upsert）✅

详细分析与调优建议见 [../performance-report.md](../performance-report.md)。

## 6. 复现步骤

```bash
# 全量接口扁平化测试（77 用例，自动 Bearer 鉴权与夹具生成）
BASE_URL=<STANDARD_BASE_URL> API_SECURITY_DEMO_USER_TOKEN=<USER_TOKEN> API_SECURITY_DEMO_ADMIN_TOKEN=<ADMIN_TOKEN> python3 scripts/run_flat_tests.py

# 导出基准
BASE_URL=<STANDARD_BASE_URL> API_SECURITY_DEMO_USER_TOKEN=<USER_TOKEN> python3 scripts/perf_bench.py --mode export --runs 3 --label exp-std-1m

# 导入基准。标准小规格环境建议先测 10 万行；百万级导入需先确认 IMPORT_MAX_ROWS_PER_TASK、IMPORT_MERGE_CHUNK_SIZE、worker、Hikari 和内存/swap。
BASE_URL=<STANDARD_BASE_URL> API_SECURITY_DEMO_USER_TOKEN=<USER_TOKEN> python3 scripts/perf_bench.py --mode import --file /tmp/perf_100k.xlsx --runs 3 --label imp-std-100k
```

结构化结果由脚本生成到 `docs/test/live-test-results.json`，该文件包含真实接口地址、重定向地址和响应片段，已加入 `.gitignore`，不纳入仓库。
